<%= bootstrap_form_for(@authorized_person) do |f| %>
  <% if @authorized_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@authorized_person.errors.count, "error") %> prohibited this authorized_person from being saved:</h2>

      <ul>
      <% @authorized_person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :full_name, :size =>"30" %>
  </div>
  <div class="field">
    <%= f.text_field :authorized_person_telephone, :size => "15" ,label: "Phone", :class=>"phone" %>
  </div>
  <div class="field">
    <%= f.hidden_field :client_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
