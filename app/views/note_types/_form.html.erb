

<div class="panel panel-default">
  <div class="panel-body">
    <%= bootstrap_form_for(@note_type) do |f| %>
        <% if @note_type.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@note_type.errors.count, "error") %> prohibited this note_type from being saved:</h2>

              <ul>
                <% @note_type.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
              </ul>
            </div>
        <% end %>

        <div class="field">
          <%= f.text_field :note_type, size: 30 %>
        </div>
        <div class="actions">
          <%= f.submit %>
        </div>
    <% end %>
  </div>
</div>