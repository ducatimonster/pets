<%= form_for(@client_pet) do |f| %>
  <% if @client_pet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@client_pet.errors.count, "error") %> prohibited this client_pet from being saved:</h2>

      <ul>
      <% @client_pet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :client_id %><br>
    <%= f.text_field :client_id %>
  </div>
  <div class="field">
    <%= f.label :pet_id %><br>
    <%= f.text_field :pet_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
