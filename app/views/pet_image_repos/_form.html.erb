

<div class="panel panel-default">
  <div class="panel-body">
    <%= bootstrap_form_for @pet_image_repo, :html=> { :multipart=>true } do |f| %>
        <% if @pet_image_repo.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@pet_image_repo.errors.count, "error") %> prohibited this pet_image_repo from being saved:</h2>

              <ul>
                <% @pet_image_repo.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
              </ul>
            </div>
        <% end %>

        <div class="field">

          <%= f.hidden_field :pet_id %>
        </div>

        <div class="field">

          <%= f.file_field :image %>
        </div>

        <div class="field">

          <%= f.text_field :comment, size:" 50" %>
        </div>
        <div class="actions">
          <%= f.submit "Upload/Update", :class=>"btn btn-default" %>
        </div>
    <% end %>
  </div>
</div>