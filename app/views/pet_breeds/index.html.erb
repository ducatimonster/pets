<div class="panel panel-default">
  <div class="panel-body">
    <p id="notice"><%= notice %></p>

    <h1>Listing Pet Breeds</h1>

    <table>
      <thead>
      <tr>
        <th>Pet</th>
        <th>Breed</th>
        <th colspan="3"></th>
      </tr>
      </thead>

      <tbody>
      <% @pet_breeds.each do |pet_breed| %>
          <tr>
            <td><%= pet_breed.pet %></td>
            <td><%= pet_breed.breed %></td>
            <!--<td><%= link_to 'Show', pet_breed %></td>-->
            <!--<td><%= link_to 'Edit', edit_pet_breed_path(pet_breed), :class=> "btn btn-warning btn-sm" %></td>-->
            <!--<td><%= link_to 'Destroy', pet_breed, method: :delete, data: { confirm: 'Are you sure?' }, :class=> "btn btn-danger btn-sm" %></td>-->
          </tr>
      <% end %>
      </tbody>
    </table>

    <br>

    <!--<h2>Import Pet Breeds</h2>-->

    <!--<%= form_tag import_pet_breeds_path, multipart: true do %>-->
        <!--<%= file_field_tag :file %>-->
        <!--<%= submit_tag "Import" %>-->
        <!--<% end %>-->

    <br>

    <%= link_to 'New Pet breed', new_pet_breed_path,  :class=>"btn btn-default" %>


  </div>
</div>

